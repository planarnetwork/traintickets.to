# Journey planner front-end

You can follow these steps to get the project up and running:

$ npm install                   # Install project dependencies
$ npm start                     # Compile and launch
